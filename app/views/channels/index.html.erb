<div>
  <h1 class="text-3xl font-bold underline">Hello, Braven!</h1>
  <ul>
    <% @channels.each do |channel| %>
      <li>
        <a href="<%= channel_path(channel) %>">
          <%= channel.name %>
        </a>
      </li>
    <% end %>
  </ul>
</div>
